name: tiff/template2
version: "2"
description: test template composition with order keyword
maintainer: nonamespace@bar.com
config:
  template: minz/nodejs_main@2
  order:
    - test
    - install
    - echo
  image: node:8
  steps:
    - echo: echo woof
    - install: echo skip
  environment:
    FOO: baz
    NODE_ENV: development
  secrets:
    - GIT_TOKEN
